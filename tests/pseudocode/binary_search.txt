function busquedaBinaria(arr, x)
    izquierda = 0
    derecha = n - 1
    while izquierda <= derecha do
        medio = (izquierda + derecha) / 2
        if arr[medio] == x then
            return medio
        end
        if arr[medio] < x then
            izquierda = medio + 1
        else
            derecha = medio - 1
        end
    end
    return -1
end
